// tipos primitivos

//booleana
var vouf = false;
console.log(typeof (vouf));

// number
var numeroQualquer = 1;
console.log(typeof (numeroQualquer));

// string
var nome = "mateus";
console.log(typeof (nome));

// function
var funcao = function () { }

// declaraçao

var variavel = 1; // escopo local e global, pode ser alterado, se nao atribuido valor, recebe null

let variavel2 = 2; // escopo local, pode ser alterado, se nao atribuido valor, recebe null

const variavel = 3; // escopo local de bloco, nao pode ser alterado, deve ter valor (da erro)

// escopo
// global: variavel disponivel todo codigo
// local: variavle disponivel só dentro do bloco/metodo

// regras
// camelcase: variavelNome
// iniciar com underscore ou cifrao: var_nome, $varNome
// nao suar reservadas

// atribuição
var nome = "mateus"; // "=" atribui valor

// comparação
// == compara dois valores
// === compara valor e tipo

// operadores
// + adição
// - subtração
// * multiplicação
// / divisão
// % divisao inteira, resto da divisão
// ** potenciação

// operadores relacionais
// > maior que
// < menor que
// >= maior igual
// <= menor igual

// operadores lógicos
// && - and
// || - or
// ! - not
